#include <stdio.h>
int fib_recursive(int n)
{
return 0;
}
    int fib_iterative(int n)
    {
    return 0;
    }
        int main()
{
    int numbers;
    printf("Enter the index to get fibbonacci series\n");
    scanf("%d" & number);
   printf("The value of fibbonacci number at a position no %d using itratively
    approach is %d\n",number,fib_itrateve (number));
   printf("The value of fibbonacci number at position no %d using recursively 
   approach is %d",number,fib_recursive (number));


return 0;
}